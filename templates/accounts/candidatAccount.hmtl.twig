{% extends 'base.html.twig' %}

{% block title 'Inscrivez-vous - Job-Ads' %}

{% block body %}
    <header class="title">
        <div class="block">
            <a href="{{ path('app_home_candidat') }}">
                <i>JOB-ADS</i>
            </a>
        </div>
    </header>
    <div class="mt-2 mb-5">
        <div class=" ">
            <div class="col-md-5 rounded-0 p-3 mx-auto signUp-box" style="background-color: #fff;">
                <h1 class="text-center account-text text-dark">Inscrivez-vous</h1>
                <span class="d-block text-center mt-4">Bénéficiez des avantages de Job-Ads : <br></span>
                <span class="d-block text-center mt-3">Vous avez déja un compte?
                    <a href="{{ path('app_login') }}">Connectez-vous!
                    </a>
                </span>
                {{ form_start(form) }}
                {{ form_row(form.nom) }}
                {{ form_row(form.prenom) }}
                {{ form_row(form.email) }}
                {{ form_row(form.dateNaissance) }}
                {{ form_row(form.telephone) }}
                {{ form_row(form.adresse) }}
                <div class="input-group">
                    <div class="input-group-prepend">
                        {{ form_label(form.localite,null,{
                            'label_attr': {
                                'class': 'input-group-text'
                            }
                        }) }}
                    </div>
                    {{ form_widget(form.localite) }}
                </div>
                {# <label for="disponibilte" class="mt-3 mb-2">Quelle est votre disponibilté en ce moment ?</label>
                {{ form_widget(form.disponibilite,{'attr':
                    {
                        'onChange':"displayDisponibility('compte_candidat_disponibilite')"
                    }}) }}
                <div class="disponibiliteYes mt-2 active">
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="disponibiliteYes" id="now" value="now"
                               onchange="disableDateDisponible('notYet')" checked>
                        <label class="form-check-label" for="now">
                            Actuellemement.
                        </label>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="disponibiliteYes" id="notYet"
                               value="notYet" onchange="disableDateDisponible('notYet')">
                        <label class="form-check-label" for="notYet">
                            A partir du
                        </label>
                        {{ form_widget( form.dateDisponibilite) }}
                    </div>
                </div> #}
                {{ form_widget(form.plainPassword) }}
                {{ form_widget(form.agreeTerms) }}

                <div class="d-flex flex-column">

                    <button class=" btn btn-outline mt-3 mb-2 text-light" type="submit"
                            style="background-color: #700598;">Créer le compte*
                    </button>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
     <script>
        //     function displayDisponibility(id) {
        //         const element = document.getElementById(id);
        //         if (element.value === '1') {
        //             document.querySelector(".disponibiliteYes").classList.remove("active");
        //             document.querySelector("#now").checked = true;

        //         } else {
        //             document.querySelector(".disponibiliteYes").classList.add("active");
        //             document.getElementById("compte_candidat_dateDisponibilite").value = null;
        //         }
        //         disableDateDisponible("notYet");
        //     }

        //     function disableDateDisponible(id) {

        //         notYet = document.getElementById(id);
        //         if (notYet.checked)
        //             document.querySelector("#compte_candidat_dateDisponibilite").disabled = false;
        //         else
        //             document.querySelector("#compte_candidat_dateDisponibilite").disabled = true;

        //     }
    </script>

{% endblock %}